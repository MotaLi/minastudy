The "one" package is include tree examples.
first: simple example.
second: synchronized example.
third: asynchronous example.
 
extend:
Exception happened if invoke the "getSession()" method when the session was closed by client.
 